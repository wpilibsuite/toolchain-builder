#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

# This has to be exported to make some magic below work.
export DH_OPTIONS

%:
	dh $@  --with autotools-dev
	
override_dh_auto_configure:
	dh_auto_configure -- -target=arm-linux-gnueabi --prefix=/usr --with-pkgversion='GDB for FRC' --disable-nls --with-libexpat --with-python=no --with-system-gdbinit=/usr/share/arm-linux-gnueabi-gdbinit --with-gdb-datadir=/usr/share/gdb-arm-linux-gnueabi

override_dh_installinfo:
	rm -rf debian/gdb-arm-linux-gnueabi/usr/share/info
	rm -rf debian/gdb-arm-linux-gnueabi/usr/share/man
	rm -rf debian/gdb-arm-linux-gnueabi/usr/include
	# this is probbably not the right dh for this, but it works as its after install
	chrpath -d debian/gdb-arm-linux-gnueabi/usr/bin/arm-linux-gnueabi-gdb

override_dh_auto_test:


